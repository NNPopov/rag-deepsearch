"""serve — transport adapters (part 3) over the web-free `rag` core.

`api.py` — FastAPI REST + SSE; `a2a.py` — A2A (Agent Card + tasks/sendSubscribe);
`app.py` — composition root (build_serve_app). The `rag` core knows nothing about transport.
"""
